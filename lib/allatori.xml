<config>
    <!--
    Encryption methods are needed during obfuscation only, they are not needed in the runtime of your application and could be located in a separate jar file not included in your distribution.

    Decryption methods are needed in the runtime, you can put them in any class(es) of your application.

    Custom string encryption feature could be used for internationalization, as Allatori will wrap all string literals (if string encryption is set to maximum) with
        methodCall("String literal")
    You can log all strings in the encryption method, translate them, and replace with internationalized version in the decryption method during the runtime.
    -->
    <input>
        <jar in="GOJUNEI-0.0.1-SNAPSHOT.jar" out="dc-sc-download.jar"/>
    </input>

    <keep-names>
        <class access="protected+">
            <field access="protected+"/>
            <method access="protected+"/>
        </class>
    </keep-names>

    <!-- 忽略的包或类，这些文件将不被混淆 -->
    <ignore-classes>
        <!-- 不要混淆主类 -->
        <class template="class com.yhgs.util.DCUtil" />
        <class template="class com.yhgs.util.SCUtil" />
<!--        <class template="class com.yhgs.util.ReaderMethodUtil" />-->
        <!-- 不要混淆第三方的代码，否则会运行jar包会报错java.lang.NoClassDefFoundError -->
    </ignore-classes>

    <property name="force-default-package" value="enable"/>
    <property name="packages-naming" value="abc"/>
    <property name="classes-naming" value="abc"/>
    <property name="classes-naming" value="unique"/>
    <property name="methods-naming" value="abc"/>
    <property name="fields-naming" value="123"/>
    <property name="fields-naming" value="iii"/>
    <property name="methods-naming-prefix" value="m_"/>
    <property name="fields-naming-prefix" value="f_"/>


    <property name="string-encryption" value="enable"/>
    <property name="string-encryption-type" value="fast"/>
    <property name="string-encryption-version" value="v4"/>
    <property name="control-flow-obfuscation" value="enable"/>
    <property name="local-variables-naming" value="single-name"/>
    <property name="synthetize-methods" value="all"/>

    <property name="log-file" value="allatori-log.xml"/>



</config>
